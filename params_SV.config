params {
    // paths to bam and bai files
    bam_path = ""
    bam = bam_path + ""
    bai = bam_path + ""

    // threads (Must be numeric.)
    threads = 8

    email = "benjamin.leopold@jax.org"
}

// vim: set ft=groovy.nextflow ts=4 sw=0 tw=100 et fdm=syntax:
